# Streamlit secrets (NEVER commit credentials / private keys)
.streamlit/secrets.toml

# Python
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
.venv/
.venv_test/
.venv_trim/
venv/
env/

# Song-trimming intermediates (originals backup + preview output)
songs_backup/
songs_trimmed/

# Local connectivity test helper
test_gsheets.py

# OS / editor cruft
.DS_Store
.idea/
.vscode/

# Temp audio / build artifacts
subs/
*.webm

# Local summary docs / generated PDFs (not part of the app)
tune_matcher_summary.pdf
PROJECT_SUMMARY.pdf
*.pdf
